/**
 * "button" element with aria-label="Minimize".
 * @param {object} props
 */
export function Minimize7({ ...props }) {
    return (
        <button aria-label="Minimize" {...props}></button>
    )
}