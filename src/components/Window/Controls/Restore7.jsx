/**
 * "button" element with aria-label="Restore".
 * @param {React.Component<"button">} props 
 * @returns {React.ReactNode}
 */
export function Restore7({ ...props }) {
    return (
        <button aria-label="Restore" {...props}></button>
    )
}