/**
 * "button" element.
 * @param {React.Component<"button">} props 
 * @returns {React.ReactNode}
 */
export function SearchButton7({ ...props }) {
    return (
        <button {...props}></button>
    )
}