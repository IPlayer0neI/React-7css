/**
 * "button" element.
 * @param {React.ComponentProps.<"button">} props 
 * @returns {React.ReactNode}
 */
export function SearchButton7({ ...props }) {
    return (
        <button {...props}></button>
    )
}